apply plugin: 'application'

mainClassName='com.jms.outbound.JmsOutApplication'

dependencies {
    compile commons['shared']

    compileOnly libraries['lombok']

    testCompile libraries['test']
}
